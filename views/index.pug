extends layout

block content
  h1= title
  h1 Ashwith Sai Anishetty
  p Welcome Home<br>
   a(href="/diary") Search Results of Diary<br>
   a(href="/grid") grid dispaly<br>
   a(href="/pick") randomitem display
  button(type="button", class="btn btn-primary", onclick="boost()") Click me
  div(id="boost") Add in extra.

  script.
    let value = 0;
    let amt = 3;
    
    function boost() {
      amt += 1;
      value += amt;
      document.getElementById("boost").innerText = `Boosted ${value} by ${amt}.`;
    }
